---
import Base from '../../layouts/Base.astro';
import NewsGrid from '../../components/NewsGrid.astro';
import { fetchAllNews, filterByCategory } from '../../data/news-bridge';
import ja from '../../i18n/ja.json';

const i = ja;
const news = await fetchAllNews();
const items = filterByCategory(news, 'thought');
---
<Base lang="ja" title={i.nav.thought} description={`${i.nav.thought} — ${i.site.tagline}`}>
  <h1>{i.nav.thought}</h1>
  <p style="color: var(--text-muted); margin-bottom: 24px;">
    Sources: Crooked Timber / 3 Quarks Daily / The Conversation (CC-BY-ND-4.0)
  </p>
  {items.length > 0 ? (
    <NewsGrid items={items} lang="ja" noImageLabel={i.card.no_image} />
  ) : (
    <p>{i.home.no_news}</p>
  )}
</Base>
