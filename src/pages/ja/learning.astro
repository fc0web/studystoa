---
import Base from '../../layouts/Base.astro';
import NewsGrid from '../../components/NewsGrid.astro';
import { fetchAllNews, filterByCategory } from '../../data/news-bridge';
import ja from '../../i18n/ja.json';

const i = ja;
const news = await fetchAllNews();
const items = filterByCategory(news, 'learning');
---
<Base lang="ja" title={i.nav.learning} description={`${i.nav.learning} — ${i.site.tagline}`}>
  <h1>{i.nav.learning}</h1>
  <p style="color: var(--text-muted); margin-bottom: 24px;">
    Sources: Hacker News 教育 stories (Algolia API 経由)
  </p>
  {items.length > 0 ? (
    <NewsGrid items={items} lang="ja" />
  ) : (
    <p>{i.home.no_news}</p>
  )}
</Base>
