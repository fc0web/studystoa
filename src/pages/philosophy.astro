---
import Base from '../layouts/Base.astro';
import NewsGrid from '../components/NewsGrid.astro';
import { fetchAllNews, filterByCategory } from '../data/news-bridge';
import en from '../i18n/en.json';

const i = en;
const news = await fetchAllNews();
const items = filterByCategory(news, 'philosophy');
---
<Base lang="en" title={i.nav.philosophy} description={`${i.nav.philosophy} — ${i.site.tagline}`}>
  <h1>{i.nav.philosophy}</h1>
  <p style="color: var(--text-muted); margin-bottom: 24px;">
    Sources: Philosophy Bites, Daily Nous, n-Category Café, PhilArchive, PhilSci-Archive.
  </p>
  {items.length > 0 ? (
    <NewsGrid items={items} lang="en" />
  ) : (
    <p>{i.home.no_news}</p>
  )}
</Base>
